#include "main.h"

/**
* main - Computes the absolute value of an integer
*
* @n: input number as an integer
*
* Return: Absolute value
*/

int -abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
